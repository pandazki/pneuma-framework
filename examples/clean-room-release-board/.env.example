# Neon Postgres connection string for the Published Application.
# Leave unset to run the app on the in-memory repository (preview / local dev).
DATABASE_URL=postgresql://user:password@host/db?sslmode=require
