# Neon Postgres for published runtimes (local publish + Vercel deploy).
DATABASE_URL=postgresql://user:password@host/db?sslmode=require
# Vercel REST API publish lane (optional — omit for local Bun publish).
VERCEL_TOKEN=
VERCEL_PROJECT=clean-room-release-board
# VERCEL_TEAM_ID=
